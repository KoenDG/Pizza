<!DOCTYPE HTML>
<html>
    <head>
        <meta charset=utf-8>
        <title>Bestellen</title>
        <link rel="stylesheet" href="src/css/reset.css">
        <link rel="stylesheet" href="src/css/global.css">
    </head>
    <body>
        {% include 'header.twig' %}
            
        <section id="choices">
            {% for option in pizzas %}
            <figure>
                <img src="src/img/{{option.name}}.jpg" height="200" width="200" alt="{{option.name}}">
                <figcaption>{{option.description}}</figcaption>
            </figure>
            {% endfor %}
        </section>
        <aside id="basket">
            <form id="bestForm" method="POST" action="bestel.php">

                <p class="formInput"><input type="submit" id="submit" name="submit" value="Verzenden" /></p>
                <input type="hidden" name="reg" value="Verzenden" />
            </form>
        </aside>
    </body>
</html>